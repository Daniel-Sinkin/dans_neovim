#!/usr/bin/env sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
venv_python="$root/knowledge/.cache/venv/bin/python"

if [ -x "$venv_python" ]; then
    exec "$venv_python" "$root/tools/knowledge.py" "$@"
fi
exec python3 "$root/tools/knowledge.py" "$@"
